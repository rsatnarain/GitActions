name: GitHub Actions Workflow
on: [push]

jobs:
  testing_github:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "Hello, $${{ github.actor }}. Welcome, to GitHub Actions!"
      - name: Display repo name
        run: echo "This workflow is running in the repo ${{ github.repository }}"